spring:
  application:
    name: Project REST
    api-v1-prefix: /api/v1.0
  datasource:
    driver-class-name:
    url: jdbc:h2:mem:test
  h2:
    console:
      enabled: true
  jpa:
    hibernate:
      ddl-auto: create
    open-in-view: false

server:
  port: 24110

logging:
  level:
    root: warn
